c_stdlib_version:  # [osx and x86_64]
  - 10.15          # [osx and x86_64]

MACOSX_SDK_VERSION:         # [osx]
  - 12.3                    # [osx]

# Needs higher stdlib for CUDNN9
c_stdlib_version:                                   # [aarch64 and cuda_compiler_version != "None"]
  - "2.28"                                          # [aarch64 and cuda_compiler_version != "None"]
docker_image:                                       # [aarch64 and cuda_compiler_version != "None"]
  - quay.io/condaforge/linux-anvil-alma-x86_64:8    # [aarch64 and cuda_compiler_version != "None"]

blas_impl:
  - mkl                         # [x86_64]
  - generic

github_actions_labels:          # [linux]
- cirun-openstack-gpu-2xlarge   # [linux]

megabuild:
- true      # [linux]
- false     # [osx]
