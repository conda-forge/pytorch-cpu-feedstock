# This file was generated automatically from conda-smithy. To update this configuration,
# update the conda-forge.yml and/or the recipe/meta.yaml.
# -*- mode: yaml -*-

name: Build conda package
on:
  push:

  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

jobs:
  build:
    name: ${{ matrix.CONFIG }}
    runs-on: ${{ matrix.runs_on }}
    timeout-minutes: 1440
    strategy:
      fail-fast: false
      max-parallel: 50
      matrix:
        include:
          - CONFIG: linux_64_blas_implgenericc_stdlib_version2.17channel_targetsconda-forge_maincuda_compiler_version12.9is_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_blas_implgenericc_stdlib_versio_ha337e3e8
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['namespace-profile-16cpu-on-linux-64']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
          - CONFIG: linux_64_blas_implgenericc_stdlib_version2.17channel_targetsconda-forge_maincuda_compiler_versionNoneis_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_blas_implgenericc_stdlib_versio_hce440a74
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['namespace-profile-16cpu-on-linux-64']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
          - CONFIG: linux_64_blas_implgenericc_stdlib_version2.28channel_targetsconda-forge_maincuda_compiler_version13.0is_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_blas_implgenericc_stdlib_versio_h6d8c6d32
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['namespace-profile-16cpu-on-linux-64']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
          - CONFIG: linux_64_blas_implmklc_stdlib_version2.17channel_targetsconda-forge_maincuda_compiler_version12.9is_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_blas_implmklc_stdlib_version2.1_h3446106f
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['namespace-profile-16cpu-on-linux-64']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
          - CONFIG: linux_64_blas_implmklc_stdlib_version2.17channel_targetsconda-forge_maincuda_compiler_versionNoneis_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_blas_implmklc_stdlib_version2.1_hb8a19044
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['namespace-profile-16cpu-on-linux-64']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
          - CONFIG: linux_64_blas_implmklc_stdlib_version2.28channel_targetsconda-forge_maincuda_compiler_version13.0is_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_64_blas_implmklc_stdlib_version2.2_h9c1f7cb3
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['namespace-profile-16cpu-on-linux-64']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
          - CONFIG: linux_aarch64_arm_variant_typesbsac_compiler_version13c_stdlib_version2.17channel_targetsconda-forge_maincuda_compiler_version12.9cxx_compiler_version13is_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_arm_variant_typesbsac_comp_hfc60734d
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['namespace-profile-16cpu-on-linux-aarch64']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
          - CONFIG: linux_aarch64_arm_variant_typesbsac_compiler_version13c_stdlib_version2.17channel_targetsconda-forge_maincuda_compiler_versionNonecxx_compiler_version13is_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_arm_variant_typesbsac_comp_h2c792143
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['namespace-profile-16cpu-on-linux-aarch64']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
          - CONFIG: linux_aarch64_arm_variant_typesbsac_compiler_version13c_stdlib_version2.28channel_targetsconda-forge_maincuda_compiler_version13.0cxx_compiler_version13is_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_arm_variant_typesbsac_comp_h6ac61af3
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['namespace-profile-16cpu-on-linux-aarch64']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
          - CONFIG: linux_aarch64_arm_variant_typetegrac_compiler_version14c_stdlib_version2.34channel_targetsconda-forge_maincuda_compiler_version12.9cxx_compiler_version14is_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: linux_aarch64_arm_variant_typetegrac_com_hbab3d77c
            UPLOAD_PACKAGES: True
            os: ubuntu
            runs_on: ['namespace-profile-16cpu-on-linux-aarch64']
            DOCKER_IMAGE: quay.io/condaforge/linux-anvil-x86_64:alma9
            tools_install_dir: ~/miniforge3
            build_workspace_dir: build_artifacts
          - CONFIG: osx_64_blas_implgenericchannel_targetsconda-forge_mainis_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: osx_64_blas_implgenericchannel_targetsco_h709642d7
            UPLOAD_PACKAGES: True
            os: macos
            runs_on: ['namespace-profile-12cpu-on-osx-arm64']
            tools_install_dir: ~/miniforge3
            build_workspace_dir: ~/miniforge3/conda-bld
          - CONFIG: osx_64_blas_implmklchannel_targetsconda-forge_mainis_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: osx_64_blas_implmklchannel_targetsconda-_h747b3c68
            UPLOAD_PACKAGES: True
            os: macos
            runs_on: ['namespace-profile-12cpu-on-osx-arm64']
            tools_install_dir: ~/miniforge3
            build_workspace_dir: ~/miniforge3/conda-bld
          - CONFIG: osx_arm64_channel_targetsconda-forge_mainis_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: osx_arm64_channel_targetsconda-forge_mai_h5f57e26b
            UPLOAD_PACKAGES: True
            os: macos
            runs_on: ['namespace-profile-12cpu-on-osx-arm64']
            tools_install_dir: ~/miniforge3
            build_workspace_dir: ~/miniforge3/conda-bld
          - CONFIG: win_64_channel_targetsconda-forge_maincuda_compiler_version12.8is_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: win_64_channel_targetsconda-forge_maincu_hca575dce
            UPLOAD_PACKAGES: True
            os: windows
            runs_on: ['namespace-profile-16cpu-on-win-64']
            tools_install_dir: D:\Miniforge
            build_workspace_dir: D:\\bld\\
          - CONFIG: win_64_channel_targetsconda-forge_maincuda_compiler_version13.0is_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: win_64_channel_targetsconda-forge_maincu_h28fca69f
            UPLOAD_PACKAGES: True
            os: windows
            runs_on: ['namespace-profile-16cpu-on-win-64']
            tools_install_dir: D:\Miniforge
            build_workspace_dir: D:\\bld\\
          - CONFIG: win_64_channel_targetsconda-forge_maincuda_compiler_versionNoneis_rcFalse
            STORE_BUILD_ARTIFACTS: True
            SHORT_CONFIG: win_64_channel_targetsconda-forge_maincu_hc68ac914
            UPLOAD_PACKAGES: True
            os: windows
            runs_on: ['namespace-profile-16cpu-on-win-64']
            tools_install_dir: D:\Miniforge
            build_workspace_dir: D:\\bld\\
    steps:

    - name: Checkout code
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

    - name: Build on Linux
      id: build-linux
      if: matrix.os == 'ubuntu'
      env:
        MINIFORGE_HOME: ${{ matrix.tools_install_dir }}
        CONDA_BLD_PATH: ${{ matrix.build_workspace_dir }}
        CONFIG: ${{ matrix.CONFIG }}
        UPLOAD_PACKAGES: ${{ matrix.UPLOAD_PACKAGES }}
        DOCKER_IMAGE: ${{ matrix.DOCKER_IMAGE }}
        CI: github_actions
        CONDA_FORGE_DOCKER_RUN_ARGS: "${{ matrix.CONDA_FORGE_DOCKER_RUN_ARGS }}"
        BINSTAR_TOKEN: ${{ secrets.BINSTAR_TOKEN }}
        FEEDSTOCK_TOKEN: ${{ secrets.FEEDSTOCK_TOKEN }}
        STAGING_BINSTAR_TOKEN: ${{ secrets.STAGING_BINSTAR_TOKEN }}
      shell: bash
      run: |
        if [[ "$(uname -m)" == "x86_64" ]]; then
          echo "::group::Configure binfmt_misc"
          docker run --rm --privileged multiarch/qemu-user-static:register --reset --credential yes
        fi
        export flow_run_id="github_$GITHUB_RUN_ID"
        export remote_url="https://github.com/$GITHUB_REPOSITORY"
        export sha="$GITHUB_SHA"
        export FEEDSTOCK_NAME="$(basename $GITHUB_REPOSITORY)"
        export GIT_BRANCH="$(basename $GITHUB_REF)"
        if [[ "${GITHUB_EVENT_NAME}" == "pull_request" ]]; then
          export IS_PR_BUILD="True"
        else
          export IS_PR_BUILD="False"
        fi
        export MINIFORGE_HOME="${MINIFORGE_HOME/#~/${HOME}}"
        export CONDA_BLD_PATH="${CONDA_BLD_PATH/#~/${HOME}}"
        echo "::endgroup::"
        ./.scripts/run_docker_build.sh

    - name: Build on macOS
      id: build-macos
      if: matrix.os == 'macos'
      env:
        MINIFORGE_HOME: ${{ matrix.tools_install_dir }}
        CONDA_BLD_PATH: ${{ matrix.build_workspace_dir }}
        CONFIG: ${{ matrix.CONFIG }}
        UPLOAD_PACKAGES: ${{ matrix.UPLOAD_PACKAGES }}
        CI: github_actions
        BINSTAR_TOKEN: ${{ secrets.BINSTAR_TOKEN }}
        FEEDSTOCK_TOKEN: ${{ secrets.FEEDSTOCK_TOKEN }}
        STAGING_BINSTAR_TOKEN: ${{ secrets.STAGING_BINSTAR_TOKEN }}
      shell: bash
      run: |
        export flow_run_id="github_$GITHUB_RUN_ID"
        export remote_url="https://github.com/$GITHUB_REPOSITORY"
        export sha="$GITHUB_SHA"
        export FEEDSTOCK_NAME="$(basename $GITHUB_REPOSITORY)"
        export GIT_BRANCH="$(basename $GITHUB_REF)"
        if [[ "${GITHUB_EVENT_NAME}" == "pull_request" ]]; then
          export IS_PR_BUILD="True"
        else
          export IS_PR_BUILD="False"
        fi
        export MINIFORGE_HOME="${MINIFORGE_HOME/#~/${HOME}}"
        export CONDA_BLD_PATH="${CONDA_BLD_PATH/#~/${HOME}}"
        ./.scripts/run_osx_build.sh

    # https://github.com/aktech/cirun-azure-resize-disk
    - name: Resize all partitions to maximum
      if: matrix.os == 'windows'
      shell: pwsh
      run: |
        Write-Output "=== RESIZING PARTITIONS ==="
        $disks = Get-Disk | Where-Object PartitionStyle -ne 'RAW'

        foreach ($disk in $disks) {
            $partitions = Get-Partition -DiskNumber $disk.Number
            foreach ($partition in $partitions) {
                try {
                    $size = Get-PartitionSupportedSize -DiskNumber $disk.Number -PartitionNumber $partition.PartitionNumber
                    if ($partition.Size -lt $size.SizeMax) {
                        Write-Output "Resizing Disk $($disk.Number) Partition $($partition.PartitionNumber) from $($partition.Size) to $($size.SizeMax)"
                        Resize-Partition -DiskNumber $disk.Number -PartitionNumber $partition.PartitionNumber -Size $size.SizeMax -Verbose
                    } else {
                        Write-Output "Disk $($disk.Number) Partition $($partition.PartitionNumber) already at max size ($($partition.Size))"
                    }
                } catch {
                    Write-Warning "Failed resizing Disk $($disk.Number) Partition $($partition.PartitionNumber): $_"
                }
            }
        }

    - name: Build on windows
      id: build-windows
      if: matrix.os == 'windows'
      shell: cmd
      run: |
        set "flow_run_id=github_%GITHUB_RUN_ID%"
        set "remote_url=https://github.com/%GITHUB_REPOSITORY%"
        set "sha=%GITHUB_SHA%"
        call ".scripts\run_win_build.bat"
      env:
        MINIFORGE_HOME: ${{ matrix.tools_install_dir }}
        CONDA_BLD_PATH: ${{ matrix.build_workspace_dir }}
        PYTHONUNBUFFERED: 1
        CONFIG: ${{ matrix.CONFIG }}
        CI: github_actions
        UPLOAD_PACKAGES: ${{ matrix.UPLOAD_PACKAGES }}
        BINSTAR_TOKEN: ${{ secrets.BINSTAR_TOKEN }}
        FEEDSTOCK_TOKEN: ${{ secrets.FEEDSTOCK_TOKEN }}
        STAGING_BINSTAR_TOKEN: ${{ secrets.STAGING_BINSTAR_TOKEN }}

    - name: Determine build outcome
      # this is to merge the status of the linux/osx/win builds into
      # something we can easily reuse during artefact generation
      id: determine-status
      if: ${{ always() }}
      shell: bash
      env:
        OS: ${{ matrix.os }}
      run: |
        if [[ "$OS" == "ubuntu" ]]; then
          STATUS=${{ steps.build-linux.outcome }}
        elif [[ "$OS" == "macos" ]]; then
          STATUS=${{ steps.build-macos.outcome }}
        elif [[ "$OS" == "windows" ]]; then
          STATUS=${{ steps.build-windows.outcome }}
        fi
        if [ -z "$STATUS" ]; then
          # steps that never ran will have empty status
          STATUS="cancelled"
        fi
        echo "status=$STATUS" >> $GITHUB_OUTPUT

    - name: Prepare conda build artifacts
      continue-on-error: true
      id: prepare-artifacts
      shell: bash
      # we do not want to trigger artefact creation if the build was cancelled
      if: ${{ always() && steps.determine-status.outputs.status != 'cancelled' && matrix.STORE_BUILD_ARTIFACTS }}
      env:
        CONDA_BLD_PATH: ${{ matrix.build_workspace_dir }}
        CI: github_actions
        CONFIG: ${{ matrix.CONFIG }}
        SHORT_CONFIG: ${{ matrix.SHORT_CONFIG }}
        JOB_STATUS: ${{ steps.determine-status.outputs.status }}
        OS: ${{ matrix.os }}
      run: |
        export MINIFORGE_HOME="${MINIFORGE_HOME/#~/${HOME}}"
        export CONDA_BLD_PATH="${CONDA_BLD_PATH/#~/${HOME}}"
        export CI_RUN_ID=$GITHUB_RUN_ID
        export FEEDSTOCK_NAME="$(basename $GITHUB_REPOSITORY)"
        export ARTIFACT_STAGING_DIR="$GITHUB_WORKSPACE"
        # Archive everything in CONDA_BLD_PATH except environments
        # Archive the CONDA_BLD_PATH environments only when the job fails
        # Use different prefix for successful and failed build artifacts
        # so random failures don't prevent rebuilds from creating artifacts.
        if [ $JOB_STATUS == "failure" ]; then
          export BLD_ARTIFACT_PREFIX="conda_artifacts"
          export ENV_ARTIFACT_PREFIX="conda_envs"
        else
          export BLD_ARTIFACT_PREFIX="conda_pkgs"
        fi
        if [ $OS == "windows" ]; then
          pwsh -Command ". '.scripts/create_conda_build_artifacts.bat'"
        else
          ./.scripts/create_conda_build_artifacts.sh
        fi

    - name: Store conda build artifacts
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
      if: ${{ always() && steps.prepare-artifacts.outcome == 'success' }}
      with:
        name: ${{ steps.prepare-artifacts.outputs.BLD_ARTIFACT_NAME }}
        path: ${{ steps.prepare-artifacts.outputs.BLD_ARTIFACT_PATH }}
        retention-days: 14
      continue-on-error: true

    - name: Store conda build environment artifacts
      uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
      # only relevant if build failed, see above
      if: ${{ always() && steps.determine-status.outputs.status == 'failure' && steps.prepare-artifacts.outcome == 'success' }}
      with:
        name: ${{ steps.prepare-artifacts.outputs.ENV_ARTIFACT_NAME }}
        path: ${{ steps.prepare-artifacts.outputs.ENV_ARTIFACT_PATH }}
        retention-days: 14
      continue-on-error: true